import{SqliteDatabase}from"../infrastructure/sqlite/database.ts";import type{PolicyOperationRecord,PolicyRepository}from"./repository.ts";import type{Policy,PolicyEvent}from"./types.ts";const C="policy";export class SqlitePolicyRepository implements PolicyRepository{readonly database:SqliteDatabase;constructor(database:SqliteDatabase){this.database=database}async find(id:string){return this.database.getState<Policy>(C,id)}async list(org:string,after:string|undefined,limit:number){return this.database.listStates<Policy>(C,org,after,limit)}async history(id:string,after=0,limit=100){return this.database.getEvents<PolicyEvent>(C,id,after,limit)}async findOperation(id:string){return this.database.getOperation<PolicyEvent>(C,id)}async commit(s:Policy,e:PolicyEvent,id:string,r:PolicyOperationRecord,create:boolean){this.database.commit({context:C,aggregateId:s.policyId,organizationId:s.organizationId,version:s.version,state:s,eventId:e.event_id,eventVersion:e.aggregate_version,event:e,operationId:id,fingerprint:r.fingerprint,create})}}
