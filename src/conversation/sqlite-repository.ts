import{SqliteDatabase}from"../infrastructure/sqlite/database.ts";import type{ConversationOperationRecord,ConversationRepository}from"./repository.ts";import type{Conversation,ConversationEvent}from"./types.ts";const CONTEXT="communication";export class SqliteConversationRepository implements ConversationRepository{readonly database:SqliteDatabase;constructor(database:SqliteDatabase){this.database=database}async find(id:string){return this.database.getState<Conversation>(CONTEXT,id)}async list(org:string,after:string|undefined,limit:number){return this.database.listStates<Conversation>(CONTEXT,org,after,limit)}async history(id:string,after=0,limit=100){return this.database.getEvents<ConversationEvent>(CONTEXT,id,after,limit)}async findOperation(id:string){return this.database.getOperation<ConversationEvent>(CONTEXT,id)}async commit(s:Conversation,e:ConversationEvent,operationId:string,r:ConversationOperationRecord,create:boolean){this.database.commit({context:CONTEXT,aggregateId:s.conversationId,organizationId:s.organizationId,version:s.version,state:s,eventId:e.event_id,eventVersion:e.aggregate_version,event:e,operationId,fingerprint:r.fingerprint,create})}}
