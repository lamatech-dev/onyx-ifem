import { SqliteDatabase } from "../infrastructure/sqlite/database.ts";
import type { ContextLinkOperationRecord, ContextLinkRepository } from "./repository.ts";
import type { ContextLink, ContextLinkEvent } from "./types.ts";
const CONTEXT="context";
export class SqliteContextLinkRepository implements ContextLinkRepository { readonly database: SqliteDatabase; constructor(database: SqliteDatabase){this.database=database;} async find(id:string){return this.database.getState<ContextLink>(CONTEXT,id);} async list(organizationId:string,afterId:string|undefined,limit:number){return this.database.listStates<ContextLink>(CONTEXT,organizationId,afterId,limit);} async history(id:string,afterVersion=0,limit=100){return this.database.getEvents<ContextLinkEvent>(CONTEXT,id,afterVersion,limit);} async findOperation(id:string){return this.database.getOperation<ContextLinkEvent>(CONTEXT,id);} async commit(state:ContextLink,event:ContextLinkEvent,operationId:string,record:ContextLinkOperationRecord,create:boolean){this.database.commit({context:CONTEXT,aggregateId:state.contextLinkId,organizationId:state.organizationId,version:state.version,state,eventId:event.event_id,eventVersion:event.aggregate_version,event,operationId,fingerprint:record.fingerprint,create});} }
