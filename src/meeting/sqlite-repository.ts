import{SqliteDatabase}from"../infrastructure/sqlite/database.ts";import type{MeetingOperationRecord,MeetingRepository}from"./repository.ts";import type{Meeting,MeetingEvent}from"./types.ts";const CONTEXT="meeting";export class SqliteMeetingRepository implements MeetingRepository{readonly database:SqliteDatabase;constructor(database:SqliteDatabase){this.database=database}async find(id:string){return this.database.getState<Meeting>(CONTEXT,id)}async list(organizationId:string,afterId:string|undefined,limit:number){return this.database.listStates<Meeting>(CONTEXT,organizationId,afterId,limit)}async history(id:string,afterVersion=0,limit=100){return this.database.getEvents<MeetingEvent>(CONTEXT,id,afterVersion,limit)}async findOperation(id:string){return this.database.getOperation<MeetingEvent>(CONTEXT,id)}async commit(state:Meeting,event:MeetingEvent,operationId:string,record:MeetingOperationRecord,create:boolean){this.database.commit({context:CONTEXT,aggregateId:state.meetingId,organizationId:state.organizationId,version:state.version,state,eventId:event.event_id,eventVersion:event.aggregate_version,event,operationId,fingerprint:record.fingerprint,create})}}
