import{SqliteDatabase}from"../infrastructure/sqlite/database.ts";import type{CapacityOperationRecord,CapacityRepository}from"./repository.ts";import type{CapacityEvent,CapacityProfile}from"./types.ts";const CONTEXT="capacity";export class SqliteCapacityRepository implements CapacityRepository{readonly database:SqliteDatabase;constructor(database:SqliteDatabase){this.database=database}async find(id:string){return this.database.getState<CapacityProfile>(CONTEXT,id)}async list(org:string,after:string|undefined,limit:number){return this.database.listStates<CapacityProfile>(CONTEXT,org,after,limit)}async history(id:string,after=0,limit=100){return this.database.getEvents<CapacityEvent>(CONTEXT,id,after,limit)}async findOperation(id:string){return this.database.getOperation<CapacityEvent>(CONTEXT,id)}async commit(s:CapacityProfile,e:CapacityEvent,operationId:string,r:CapacityOperationRecord,create:boolean){this.database.commit({context:CONTEXT,aggregateId:s.capacityProfileId,organizationId:s.organizationId,version:s.version,state:s,eventId:e.event_id,eventVersion:e.aggregate_version,event:e,operationId,fingerprint:r.fingerprint,create})}}
