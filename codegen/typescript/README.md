# ONYX contracts for TypeScript

Canonical TypeScript types for every ONYX IFEM v2.0 command and event, including generic command/event envelopes and shared actor, authority, audit, object-reference, timestamp, UUIDv7, and vector-clock representations.

```sh
npm ci
npm run check
npm run build
```
