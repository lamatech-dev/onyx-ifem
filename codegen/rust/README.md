# ONYX contracts for Rust

Canonical serializable Rust types for every ONYX IFEM v2.0 command and event, including command/event envelopes and shared actor, authority, audit, object-reference, timestamp, UUIDv7, and vector-clock representations.

```sh
cargo fmt -- --check
cargo test --locked
```
